"use client";

// Stub — replaced by the full combobox port backed by /api/search.
export function SearchDropdownComponent() {
  return (
    <input
      type="search"
      placeholder="Search..."
      className="w-full rounded border border-input px-2 py-1 font-sans text-sm sm:w-[300px]"
    />
  );
}
