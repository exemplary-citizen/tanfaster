{
  "nodeModulesDir": "auto",
  "tasks": {
    "dev": "vite dev",
    "build": "vite build && deno run -A scripts/patch-builtins.ts",
    "start": "deno run -A .output/server/index.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
